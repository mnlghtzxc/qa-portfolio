# requirements.txt — список зависимостей Python-приложения
# Каждая строка содержит имя пакета, который будет установлен командой `pip install -r requirements.txt`

# Веб-фреймворк для создания API (маршруты, обработка запросов, jsonify)
flask

# Драйвер для подключения к PostgreSQL из Python (бинарная сборка, не требует компилятора)
psycopg2-binary
